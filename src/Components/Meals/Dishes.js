import Card from "../UI/Card";
import DishItem from "./DishItem";

const Meals = (props) => {

  return (
    <Card className="">
      <li>props.list.map((i)=>{<DishItem title={} detail={} price={}/>})</li>
    </Card>
  );
};
export default Meals;
